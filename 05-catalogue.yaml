- name: configuring catalogue server
  hosts: catalogue
  become: yes
  tasks:
  - name: enable nodejs 20
    command: dnf module disable nodejs -y
  - name: install nodejs 20
    package:
       name: nodejs
       state: present
  - name: create roboshop system user
    user:
      name: roboshop
      system: true
      home: /app
      shell: /sbin/nologin
      comment: "Roboshop system user"
  - name: remove app directory
    file: 
      path: /app
      state: absent
  - name: delete catalogue.zip in tmp directory
    file:
      path: /tmp/catalogue.zip
      state: absent
  - name: create app directory
    file:
      path: /app 
      state: directory
  - name: download catalogue
    get_url:
      url: https://roboshop-artifacts.s3.amazonaws.com/catalogue-v3.zip
      dest: /tmp/catalogue.zip
  - name: unzip catalogue
    unarchive:
          src: /tmp/catalogue.zip
          dest: /app
          remote_src: yes
  - name: install dependencies
    community.general.npm:
        path: /app
  - name: copy catalogue service
    template:
       src: catalogue.service.j2 # this is the template which will run in runtime asible will search for service file in run time...ansible executes the variable assigned in service template in run time 
       dest: /etc/systemd/system/catalogue.service
  - name: copy mongodb repo
    copy: 
      src: mongo.repo
      dest: /etc/yum.repos.d/mongo.repo
  - name: install mongodb client
    package:
       name: mongodb-mongosh
       state: present
  - name: check catalogue schema exist or not
    shell: mongosh --host "{{ MONGODB_HOST }}" --eval 'db.getMongo().getDBNames().indexOf("catalogue")'
    register: catalogue_output
  - name: print catalogue_output
    debug: "{{ catalogue_output }}"